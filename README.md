# Count Files Utility

Простий Bash-скрипт для підрахунку кількості файлів у директорії `/etc`. Проект демонструє автоматизацію збірки пакетів для Linux-дистрибутивів.

## 📋 Опис
Скрипт перевіряє наявність директорії `/etc`, підраховує кількість файлів у ній та виводить результат. Проект включає конфігурацію для створення інсталяційних пакетів **DEB** та **RPM**, а також налаштування CI/CD.

## 🚀 Використання
Запуск скрипта вручну:
```bash
chmod +x count_files.sh
./count_files.sh
```

## 📦 Пакування
Проект підтримує збірку пакетів:

* **DEB** (для Debian/Ubuntu) — використовує `debhelper`.
* **RPM** (для RHEL/Fedora) — використовує `rpmbuild` та `spec`-файл.

## ⚙️ CI/CD
Автоматизація реалізована двома способами:

1. **GitHub Actions**: Воркфлоу для збірки та завантаження RPM/DEB артефактів.
2. **Jenkins**: Конфігурація через Docker (Master/Agent) та `Jenkinsfile` для повного циклу збірки й тестування.

## 📝 Автор та Ліцензія
* **Автор:** Illya Moskat
* **Ліцензія:** MIT
